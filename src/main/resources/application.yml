spring:
  application:
    name: 8ocket

  datasource:
    url: jdbc:mysql://localhost:3306/8ocket
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health

server:
  port: 8080